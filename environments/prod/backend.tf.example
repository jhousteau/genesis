# Backend Configuration for Production Environment
# Copy this file to backend.tf after bootstrap is complete

terraform {
  backend "gcs" {
    bucket = "tf-state-123456789012-bootstrap"  # Replace with your bootstrap state bucket
    prefix = "prod/terraform/state"
  }
}
